# Kinglet language server + VS Code extension (Perch).

# Bootstrap compiler frontend (symlink/junction via scripts/setup-deps)
/third_party/bootstrap
/build/config
/build/toolchain
/src

# Build outputs
/out/
bin/

/node_modules/
*.vsix
.DS_Store
test.kl
tmp-err.txt
tmp-*.txt
